import PropTypes from 'prop-types';
export const demographicProps = {
  values: PropTypes.object,
  errors: PropTypes.object,
  handleChange: PropTypes.func,
  handleBlur: PropTypes.func,
  setShowLoginPopup: PropTypes.func,
};
