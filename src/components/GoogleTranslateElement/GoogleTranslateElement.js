import React from 'react';
import * as styles from './google-translate-element.module.scss';
// import PropTypes from 'prop-types';

const GoogleTranslateElement = () => {
  return <span id="google_translate_element" className={styles.translateContainer} />;
};

GoogleTranslateElement.propTypes = {};

export default GoogleTranslateElement;
